name: stats_analyzer
description: DiamondEdge Stat Analyzer - Advanced Sports Analytics Platform with AI/ML
version: 2.4.1
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  shared_preferences: ^2.2.1
  path_provider: ^2.1.1
  connectivity_plus: ^5.0.1
  http: ^1.1.0
  web_socket_channel: ^2.4.0
  dio: ^5.3.2
  fl_chart: ^0.62.0
  flutter_local_notifications: ^15.1.1
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  flutter_svg: ^2.0.7
  intl: ^0.18.1
  equatable: ^2.0.5
  json_annotation: ^4.8.1
  collection: ^1.17.2
  sqflite: ^2.3.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
