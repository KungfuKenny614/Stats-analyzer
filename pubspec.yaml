name: stat_analyzer
description: Advanced Sports Analytics Platform with AI
version: 2.4.1
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  fl_chart: ^0.64.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
