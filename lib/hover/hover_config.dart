import 'dart:async';

class HoverConfig {
  static const Duration hoverDelay = Duration(milliseconds: 200);
  static const Duration fadeOutDelay = Duration(milliseconds: 150);
  static const Duration animationDuration = Duration(milliseconds: 180);

  static const double maxWidth = 420.0;
  static const double minWidth = 340.0;
  static const double maxHeight = 560.0;
  static const double borderRadius = 16.0;
  static const double shadowElevation = 8.0;
}
